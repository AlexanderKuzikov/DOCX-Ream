# DOCX-Ream 📄

**DOCX-Ream** (от англ. *Ream* — стопа бумаги) — инструмент для пакетного тестирования и QA шаблонов документов. 
Он автоматизирует генерацию DOCX файлов через API "Kombinator", организует их в удобную структуру папок и создает информативные обложки для визуальной проверки и распечатки.

![Node.js](https://img.shields.io/badge/Node.js-18.x-green) ![License](https://img.shields.io/badge/License-MIT-blue)

## 🚀 Возможности

- **Манифест-ориентированный подход:** Полный контроль над тем, какие шаблоны на каких данных проверяются, через единый файл `manifest.json`.
- **Умная структура папок:** Автоматически создает директории вида `Имя Кейса - ID`, что упрощает навигацию.
- **Генерация обложек:** Создает файл `_ОБЛОЖКА.docx` для каждого пакета документов. Обложка содержит дату прогона, список датасетов и ID шаблона — идеально для печати "стопкой".
- **Пакетная обработка:** Один шаблон прогоняется через множество наборов данных (Civil, Arbitr, Complex) одной командой.
- **Безопасность:** Поддержка локальной конфигурации для защиты учетных данных.

## 🛠 Установка

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/AlexanderKuzikov/docx-ream.git
   cd docx-ream
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. **Настройка безопасности:**
   Создайте в корне файл `auth.json` (он добавлен в `.gitignore`, чтобы ваши пароли не попали в репозиторий):
   ```json
   {
     "email": "your-email@example.com",
     "password": "your-password"
   }
   ```

## ⚙️ Использование

### 1. Подготовка данных
Поместите ваши JSON-файлы с тестовыми данными в папку `/data`. 
*Пример: `civil.json`, `arbitr.json`, `complex.json`.*

### 2. Настройка Манифеста
Отредактируйте `manifest.json`, чтобы определить задачи для генерации:

```json
[
  {
    "id": 729,
    "case": "Ходатайство в отсутствие (Общий)",
    "datasets": ["civil", "arbitr", "complex"]
  },
  {
    "id": 812,
    "case": "Ходатайство в отсутствие (Аренда ТС)",
    "datasets": ["civil", "arbitr"]
  }
]
```

### 3. Запуск
Запустите скрипт:
```bash
npm start
```

### 4. Результат
В папке `/output` появятся готовые к проверке пакеты документов:

```text
output/
├── Ходатайство в отсутствие (Общий) - 729/
│   ├── _ОБЛОЖКА.docx        <-- Печатать первым
│   ├── arbitr.docx
│   ├── civil.docx
│   └── complex.docx
└── ...
```

## 🔒 Безопасность

Проект настроен так, чтобы исключить случайную утечку конфиденциальных данных:
- `auth.json`, `.env`, `config.local.js` исключены из Git.
- Папка `output/` (сгенерированные документы) исключена из Git.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).
